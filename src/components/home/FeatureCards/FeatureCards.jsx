import "./FeatureCards.css";

export default function FeatureCards() {
  return (
    <section
      style={{
        border: "1px dashed var(--ts-border)",
        padding: 12,
        borderRadius: 12,
        marginTop: 12,
      }}
    >
      <h2>Feature Cards</h2>
      <p>placeholder</p>
    </section>
  );
}
